<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.unomas.mapper.BoardMapper">
  <insert id="create">
		insert into test_board(faq_title,faq_content,faq_cate) 
		values(#{faq_title},#{faq_content},#{faq_cate})
	</insert>
	
	<select id="listAll" resultType="BoardVO">
		<![CDATA[
		select * from test_board 
		where faq_num > 0
		order by faq_num desc
		]]>
	</select>
	
	<select id="sortCategory" resultType="BoardVO">
		select * from test_board
		where faq_cate=#{faq_cate}
		order by faq_num desc
		limit 1,10
	</select>
	
	<select id="sortCateCount">
		select count(*) test_board
		where faq_num > 0 and faq_cate=#{faq_cate}
	</select>
	
	<select id="countBoard" resultType="Integer">
		<![CDATA[
		select count(*) from test_board
		where faq_num > 0
		]]>
		<include refid="search_condition"></include>
	</select>
	
	<select id="pagingBoard" resultType="BoardVO">
		<![CDATA[
		select * from test_board
		where faq_num > 0
		]]>
		<include refid="search_condition"></include>
		order by faq_num desc
		limit #{pageStart} , #{perPageNum}
	</select>
	
	<select id="getBoard" resultType="BoardVO">
		select * from test_board
		where faq_num=#{faq_num}
	</select>
	
	<update id="updateBoard">
		update test_board 
		set faq_title=#{faq_title},faq_cate=#{faq_cate},faq_content=#{faq_content} 
		where faq_num=#{faq_num}
	</update>
	
	<delete id="deleteBoard">
		delete from test_board
		where faq_num=#{faq_num}
	</delete>
	
	<sql id="search_condition">
	<if test="search_type != null">
		<if test="search_type == 'title'.toString()">
			and faq_title like concat('%', #{keyword}, '%')
		</if>
		<if test="search_type == 'cate'.toString()">
			and faq_cate like concat('%', #{keyword}, '%')
		</if>
		<if test="search_type == 'content'.toString()">
			and faq_content like concat('%', #{keyword}, '%')
		</if>
	</if>
	</sql>
	
</mapper>
